<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.re_z">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Rez">
        <activity android:name=".Activities.Activity_Main"
            android:label="@string/app_name"
            android:theme="@style/Theme.Rez">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Activities.Activity_Access"
            android:label="@string/app_name"
            android:theme="@style/Theme.Rez.NoActionBar">

        </activity>

        <activity android:name=".Activities.Recetas.Activity_Agregar"
            android:label="@string/app_name"
            android:theme="@style/Theme.Rez">
        </activity>

        <activity android:name=".Activities.Recetas.Activity_Detalles"
            android:label="@string/app_name"
            android:theme="@style/Theme.Rez">
        </activity>

        <activity android:name=".Activities.Recetas.Activity_Editar"
            android:label="@string/app_name"
            android:theme="@style/Theme.Rez">
        </activity>

        <activity android:name=".Activities.Comentarios.Activity_Editar"
            android:label="@string/app_name"
            android:theme="@style/Theme.Rez">
        </activity>
        <activity android:name=".Activities.Comentarios.Activity_Agregar"
            android:label="@string/app_name"
            android:theme="@style/Theme.Rez">
        </activity>
    </application>

</manifest>